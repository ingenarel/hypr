# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,1440x900,auto,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$menu = wofi --height=90% --width=300 --show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:
#exec-once = /usr/lib/polkit-gnome-authentication-agent-1 
# exec-once = usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# exec-once = gsettings set org.gnome.desktop.interface cursor-theme "ArchCursorTheme"
# exec-once = mountall
# exec-once = [workspace special:magic silent] sleep 60 && keepassxc
# exec-once = wl-paste --type text --watch cliphist store
# exec-once = wl-paste --type image --watch cliphist store
# exec-once = sleep 60 && webcord --start-minimized
# exec-once = nm-applet &
exec-once = /usr/lib/polkit-kde-authentication-agent-1 & swww-daemon & swaync & waybar & mountall ; mpd
# exec-once = [workspace special:magic silent; float; size 90% 10%; center] sleep 300 && $terminal -e java -jar ~/Sheepit/sheepitclient.jar -ui oneLine
# exec-once = nicotine --headless

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
# env = QT_QPA_PLATFORMTHEME,qt6ct
env = XCURSOR_THEME,ArchCursorTheme
env = GTK_THEME,Arc-Dark-solid

# env = HYPRLAND_LOG_WLR,1

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 0 #5
    gaps_out = 0 #20

    border_size = 2 #2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgb(ff00ff)
    col.inactive_border = rgb(850045)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true #false 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 9

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    drop_shadow = false
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        # enabled = true
        enabled = false
        # size = 3
        size = 3
        # passes = 1
        passes = 3
        
        # vibrancy = 0.1696
        vibrancy = 0
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true
    first_launch_animation = false

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = myElasticBezier, 0.68, -0.55, 0.27, 1.55
    # bezier = myElasticBezier, 0, 0, 0, 1
    # animation = windows, 1, 7, myBezier
    animation = windows, 1, 3, myElasticBezier
    # animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 3, myElasticBezier, slidevert
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    # pseudotile = false # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
    # preserve_split = false # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
# master {
#     new_status = false
# }

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
    middle_click_paste = false
}

#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    # touchpad {
    #     natural_scroll = false
    # }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
# device {
#     name = epic-mouse-v1
#     sensitivity = -0.5
# }


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

# execution
    # terminal
        bind = $mainMod, Q, exec, $terminal
        bind = $mainMod SHIFT, Q, exec, [float; size 90% 90%; center] $terminal
    # terminal
    # email
        bind = $mainMod, E, exec, $terminal -e neomutt
        bind = $mainMod SHIFT, E, exec, [float; size 90% 90%; center] $terminal -e neomutt
    # email
    # file manager
        bind = $mainMod, F, submap, file_manager
        submap = file_manager
            bind =, Y, exec, $terminal -e yazi
            bind =, Y, submap, reset
            bind =, D, exec, dolphin
            bind =, D, submap, reset
            bind =, F, submap, float_file_manager
            bind =, F, submap, reset
            bind =,catchall,submap,reset
        submap = reset
        submap = float_file_manager
            bind =, Y, exec, [float; size 90% 90%; center] $terminal -e yazi
            bind =, Y, submap, reset
            bind =, D, exec, [float; size 90% 90%; center] dolphin
            bind =, D, submap, reset
            bind =,catchall,submap,reset
        submap = reset
    # file manager
    # editor
        bind = $mainMod, N, exec, $terminal -e nvim
        bind = $mainMod SHIFT, N, exec,[float; size 90% 90%; center] $terminal -e nvim
    # editor
    # launchers
        bind = $mainMod, R, exec, $menu,
        bind = $mainMod, C, exec, python ~/.config/wofi/chats.py
        bind = ALT, F4, exec, python ~/.config/wofi/shutdown.py
    # launchers
    # music
        bind = $mainMod, backspace, submap, music_control
        submap = music_control
            #mpc
                bind =, space, exec, mpc toggle
                binde =, equal, exec, mpc volume +1 && swaync-client --hide-latest && notify-send "$(mpc volume)"
                binde =, minus, exec, mpc volume -1 && swaync-client --hide-latest &&  notify-send "$(mpc volume)"
                binde =, period, exec, mpc next && swaync-client --hide-latest && notify-send "$(mpc status)"
                binde =, comma, exec, mpc prev && swaync-client --hide-latest && notify-send "$(mpc status)"
                binde =, r, exec, mpc repeat && swaync-client --hide-latest && notify-send "$(mpc status)"
                binde =, y, exec, mpc single && swaync-client --hide-latest && notify-send "$(mpc status)"
                binde =, z, exec, mpc random && swaync-client --hide-latest && notify-send "$(mpc status)"
            #mpc
            bind =, n, exec, [float; size 80% 80%; center] killall cava ; $terminal -e ncmpcpp
            bind =, n, submap, reset
            bind =, c, exec, [float; size 80% 80%; center] killall ncmpcpp ; $terminal -c ~/.config/kitty/kitty_1_pts.conf -e cava
            bind =, c, submap, reset
            bind =, catchall, submap, reset
        submap = reset
    # music
    # audio
        binde = $mainMod, equal, exec, pactl set-sink-volume @DEFAULT_SINK@ +1%
        binde = $mainMod, minus, exec, pactl set-sink-volume @DEFAULT_SINK@ -1%
    # audio
# execution
# navigation
    # window navigation
        bind = $mainMod, M, submap, move_in_windows
        submap = move_in_windows
            binde =, H, movefocus, l
            binde =, L, movefocus, r
            binde =, K, movefocus, u
            binde =, J, movefocus, d
            bind =, catchall, submap,reset
        submap = reset
    # window navigation
    # window move
        bindm = $mainMod, mouse:272, movewindow # left mouse button
        bindm = $mainMod, mouse:273, resizewindow # right mouse button
    # window move
    # window edit
        bind = $mainMod, escape, killactive,
        bind = $mainMod SHIFT, V, togglefloating,
        bind = $mainMod, P, pseudo # dwindle
        bind = $mainMod SHIFT, J, togglesplit # dwindle
        # fullscreen stuff
            bind = $mainMod, F11, submap, enter_fullscreenstate_mode
            submap = enter_fullscreenstate_mode
                bind =, 0, submap, fullscreenstate_mode_0
                bind =, 1, submap, fullscreenstate_mode_1
                bind =, 2, submap, fullscreenstate_mode_2
                bind =, 3, submap, fullscreenstate_mode_3
                bind =, catchall, submap, reset
            submap = reset
            submap = fullscreenstate_mode_0
                bind =, 0, fullscreenstate, 0 0
                bind =, 0, submap, reset
                bind =, 1, fullscreenstate, 0 1
                bind =, 1, submap, reset
                bind =, 2, fullscreenstate, 0 2
                bind =, 2, submap, reset
                bind =, 3, fullscreenstate, 0 3
                bind =, 3, submap, reset
                bind =, catchall, submap, reset
            submap = reset
            submap = fullscreenstate_mode_1
               bind =, 0, fullscreenstate, 1 0
               bind =, 0, submap, reset
               bind =, 1, fullscreenstate, 1 1
               bind =, 1, submap, reset
               bind =, 2, fullscreenstate, 1 2
               bind =, 2, submap, reset
               bind =, 3, fullscreenstate, 1 3
               bind =, 3, submap, reset
               bind =, catchall, submap, reset
            submap = reset
            submap = fullscreenstate_mode_2
               bind =, 0, fullscreenstate, 2 0
               bind =, 0, submap, reset
               bind =, 1, fullscreenstate, 2 1
               bind =, 1, submap, reset
               bind =, 2, fullscreenstate, 2 2
               bind =, 2, submap, reset
               bind =, 3, fullscreenstate, 2 3
               bind =, 3, submap, reset
               bind =, catchall, submap, reset
            submap = reset
            submap = fullscreenstate_mode_3
               bind =, 0, fullscreenstate, 3 0
               bind =, 0, submap, reset
               bind =, 1, fullscreenstate, 3 1
               bind =, 1, submap, reset
               bind =, 2, fullscreenstate, 3 2
               bind =, 2, submap, reset
               bind =, 3, fullscreenstate, 3 3
               bind =, 3, submap, reset
               bind =, catchall, submap, reset
            submap = reset
        # fullscreen stuff
    # window edit
    # workspace nav
        bind = $mainMod, 1, workspace, 1
        bind = $mainMod, 2, workspace, 2
        bind = $mainMod, 3, workspace, 3
        bind = $mainMod, 4, workspace, 4
        bind = $mainMod, 5, workspace, 5
        bind = $mainMod, 6, workspace, 6
        bind = $mainMod, 7, workspace, 7
        bind = $mainMod, 8, workspace, 8
        bind = $mainMod, 9, workspace, 9
        bind = $mainMod, 0, workspace, 10
        bind = $mainMod, right, workspace, e+1
        bind = $mainMod, left, workspace, e-1
    # workspace nav
    # workspace move
        bind = $mainMod SHIFT, 1, movetoworkspace, 1
        bind = $mainMod SHIFT, 2, movetoworkspace, 2
        bind = $mainMod SHIFT, 3, movetoworkspace, 3
        bind = $mainMod SHIFT, 4, movetoworkspace, 4
        bind = $mainMod SHIFT, 5, movetoworkspace, 5
        bind = $mainMod SHIFT, 6, movetoworkspace, 6
        bind = $mainMod SHIFT, 7, movetoworkspace, 7
        bind = $mainMod SHIFT, 8, movetoworkspace, 8
        bind = $mainMod SHIFT, 9, movetoworkspace, 9
        bind = $mainMod SHIFT, 0, movetoworkspace, 10
    # workspace move
    # scratchpad
        bind = $mainMod, S, togglespecialworkspace, magic
        bind = $mainMod SHIFT, S, movetoworkspace, special:magic
    # scratchpad
    # taskbar
        bind = $mainMod, T, submap, taskbar
        submap = taskbar
            bind =, r, exec, killall waybar && waybar &
            bind =, t, exec, pgrep waybar >/dev/null && killall waybar || waybar &
            bind =, catchall, submap, reset
        submap = reset
    # taskbar
# navigation

# bind = $mainMod, M, exit,
# bind = $mainMod, V, exec, cliphist list | wofi -S dmenu | cliphist decode | wl-copy,
bind = $mainMod, Print, exec, XDG_CURRENT_DESKTOP=Sway && flameshot gui

#remote:/ Example special workspace (scratchpad)

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
windowrulev2 = float, class:(vesktop)
windowrulev2 = size 90% 90%, class:(vesktop)
windowrulev2 = center, class:(vesktop)

windowrulev2 = float, class:(flameshot)

windowrulev2 = float, class:(KeePassXC)

# windowrulev2 = size 90% 90%, title:(discordo)
# windowrulev2 = center, title:(discordo)
# windowrulev2 = float, size 90% 90%, center, class:^(discord)$

# Example windowrule v2
# windowrulev2 = float,size [90%] [90%], class:^(discord)$,title:^(Discord)$

windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
